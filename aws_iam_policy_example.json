{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "AllowS3WeatherDataAccess",
      "Effect": "Allow",
      "Action": [
        "s3:PutObject",
        "s3:GetObject",
        "s3:ListBucket"
      ],
      "Resource": [
        "arn:aws:s3:::YOUR_BUCKET_NAME",
        "arn:aws:s3:::YOUR_BUCKET_NAME/*"
      ]
    }
  ]
}

# Instructions:
# 1. Replace YOUR_BUCKET_NAME (in both Resource lines) with your actual S3 bucket name
# 2. Copy this entire JSON (without these comments) into AWS IAM Console
# 3. Go to IAM → Users → karthik-aws → Add permissions → Create inline policy → JSON tab
# 4. Paste the updated policy and save as "S3WeatherDataAccess"

